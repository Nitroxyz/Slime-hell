///@desc starts the loop for second path and shooting

if(path_index = -1) {
	if(left_side = true) {
		simulhardloop(true);
	} else {
		simulhardloop(false);	
	}
	alarm[0] = 20;
}
	else {
	timeline_position -= 2;	
	}
